-- Ensure the database exists with correct charset (user/db are created by MYSQL_DATABASE env var)
CREATE DATABASE IF NOT EXISTS expense_system_development CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;